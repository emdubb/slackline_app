<h1>Lines</h1>

post a line

<table>
<% @lines.each do |line| %>
  <tr>
    <td><%= line.brand %></td>
    <td><%= line.length %>ft</td>
    <td><%= line.width %>in</td>
    <td><%= line.style %> Line</td>
    <td><%= line.system %></td>
  </tr>
<% end %>
</table>
